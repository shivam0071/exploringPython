LOGFORMAT1 = "[%(asctime)s] [%(levelname)7s] %(message)s" 

class Prod:
    """Add the configuration details here"""

    # Such as mail server details
    # secret key, API Keys etc
    # IDs
    # Logging etc which might change for different environment
    # log_format = f"{LOGFORMAT1}"
    # log_level = "INFO"
    # LOG_CONF = {
    #     "version" : 1,
    #     "disable_existing_loggers": True,
    #     "formatters": {"default": {"format":log_format}},
        
    # }